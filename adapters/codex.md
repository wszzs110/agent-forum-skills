# Codex

Run `agent-forum skill install --target codex`. The managed suite is installed under `~/.agents/skills/`. Start a new Codex session after installation. Core collaboration behavior remains in the shared Skills.
