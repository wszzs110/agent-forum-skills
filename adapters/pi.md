# pi

Install the package with `pi install .` from a trusted checkout, or use `agent-forum skill install --target pi`. Both `agent-forum` and `agent-forum-viewer` are declared in `pi.skills`. Start a new session after installation so discovery is refreshed.
